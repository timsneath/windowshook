name: windowshook
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.18.0-44.1.beta <3.0.0'

dependencies:
  ffi: ^2.0.1
  flutter:
    sdk: flutter
  win32: ^2.7.0

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
